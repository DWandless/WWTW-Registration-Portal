name: Keep Streamlit App Alive

on:
  schedule:
    - cron: "0 */12 * * *" 
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: |
          curl --silent --max-time 30 https://wwtw-registration.streamlit.app/_stcore/health
